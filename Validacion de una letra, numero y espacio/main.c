#include <stdio.h>
#include <stdlib.h>

int main()
{


   /*

   char romanos[]={'I','V','X','C','M'};

    int isalpha(char) esto me dice si es una letra o no

   int isDigit(char) esto me dice si es digito o no si es un numero

   int isSpace(char) esto me dice si tiene espacio o no */

  /* printf("Ingrese un dato");

   gris(auxstring);

   get=ValidoStringgenero(auxstring)

     if(get==1)
     {
         edad=atoi(auxstring);

     }

     else
     {

     }
    return 0;*/
}
